<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join Game - Christmas Crab Game</title>
    <link href="/css/style.css" rel="stylesheet">
</head>
<body class="bg-red-900 min-h-screen flex items-center justify-center vintage-texture">
    <div class="bg-red-950 rounded-lg shadow-2xl p-8 max-w-md w-full mx-4 vintage-texture cartoon-border texture-overlay relative">
        <div class="relative z-10">
            <h1 class="text-4xl font-bold text-white text-center mb-8">🎄 Christmas Crab Game 🦀</h1>
            
            <% if (error) { %>
                <div class="bg-red-600 text-white p-4 rounded mb-4 cartoon-border">
                    <%= error %>
                </div>
            <% } %>

            <form method="POST" action="/join" class="space-y-4">
                <div>
                    <label for="username" class="block text-white text-sm font-medium mb-2">
                        Enter Your Username
                    </label>
                    <input 
                        type="text" 
                        id="username" 
                        name="username" 
                        required
                        maxlength="20"
                        class="w-full px-4 py-3 rounded-lg bg-red-800 text-white placeholder-red-300 border-2 border-red-600 focus:border-red-400 focus:outline-none cartoon-border"
                        placeholder="Your username"
                        autofocus
                    >
                </div>
                
                <div>
                    <label for="fcAmount" class="block text-white text-sm font-medium mb-2">
                        Enter FC Amount (Points of Credit)
                    </label>
                    <input 
                        type="number" 
                        id="fcAmount" 
                        name="fcAmount" 
                        required
                        min="1"
                        max="100000"
                        value="1000"
                        class="w-full px-4 py-3 rounded-lg bg-red-800 text-white placeholder-red-300 border-2 border-red-600 focus:border-red-400 focus:outline-none cartoon-border"
                        placeholder="Enter FC amount"
                    >
                </div>
                
                <button 
                    type="submit"
                    class="w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200 cartoon-border"
                >
                    Join Game
                </button>
            </form>

            <p class="text-red-200 text-sm text-center mt-6">
                Fun Christmas-themed betting game for your team!
            </p>
        </div>
    </div>
</body>
</html>

